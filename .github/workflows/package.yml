name: Package

on:
  workflow_dispatch:

jobs:
  package:
    uses: InstechSandbox/.github/.github/workflows/reusable-docker-build.yml@ee7c589b6edeb479707d21c3d256092319e7af88
    with:
      image-name: eudi-web-verifier
      dockerfile: Dockerfile
      push: false
      tags: |
        ghcr.io/instechsandbox/eudi-web-verifier:${{ github.sha }}
        ghcr.io/instechsandbox/eudi-web-verifier:main